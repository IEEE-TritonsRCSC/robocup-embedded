# .clangd cache, which is just for personal LSP purposes.
.cache
# object files, which should be generated by make for each computer for compatibility reasons
**/*.o
# Cyclomatic complxity profiles, removed in the current makefile configuration
**/*.cyclo
# Stack usage profiles
**/*.su 
compile_commands.json
